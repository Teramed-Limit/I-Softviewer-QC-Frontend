import React from 'react';

import classes from './DicomViewer.module.scss';

const DicomViewer = () => {
    return (
        <div className={classes.Template}>
            <h1>DicomViewer component</h1>
        </div>
    );
};

export default DicomViewer;
